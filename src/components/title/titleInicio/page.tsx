import { interClass } from "@/fonts/inter";

export default function TitleInicioComponent() {
  return (
    <div className={`${interClass}`}>
      <h1 className="text-3xl sm:text-5xl font-[700] text-black mt-10 text-center">
        Ask everything you want!
      </h1>
    </div>
  );
}
